# 2026.241.1038 — feat(status): read the local install ledger from the `ledger:` section of the per-host charly.yml

## Summary

The install ledger lives in the `ledger:` section of the per-host charly.yml (sdk#183) — the single home for local system state — NOT in per-deploy JSON files under `~/.config/opencharly/installed/`. The local status collector reads the ledger from there: `ledgerDeployIDs`/`ledgerCandyNames` extract the deploy-ids and candy names from the charly.yml, and the rows are synthesized from the `DeployRecord`/`CandyRecord` sets as before. The availability gate now requires BOTH maps empty (candy records alone can synthesize rows).

- **`status_local.go`**: reads the ledger from the charly.yml `ledger:` section (replacing the `paths.Deploys`/`paths.Candies` JSON-file reads).
- **`status_test.go`**: `withTempLedger` uses the new `ConfigFile` shape.
- **`go.mod`**: sdk v0.2026241.1032 (the ledger-in-charly.yml tag).

## How tested

```
$ cd candy/plugin-substrate && go build ./... && echo BUILD OK
BUILD OK
$ go test ./...
ok  	github.com/opencharly/plugin-substrate/candy/plugin-substrate	0.004s
?   	github.com/opencharly/plugin-substrate/candy/plugin-substrate/cmd/serve	[no test files]
```

**Disposable fresh-rebuild bed run (A2/R10)** — `check-charly-vm` (the `disposable: true` Arch VM bed). The charly binary was rebuilt from the committed tree with this plugin change (local replace) + the sdk#183 ledger change, synced to `candy/charly-dev/bin/charly`, and the bed run:

```
$ charly check run check-charly-vm
charly check run check-charly-vm: [vm-build] PASS after 4.7s [allowlisted=1]
charly check run check-charly-vm: [vm-create] PASS after 8.0s [allowlisted=1]
charly check run check-charly-vm: [deploy-add] PASS after 1m8s [allowlisted=5]
charly check run check-charly-vm: [bring-up-members] PASS
charly check run check-charly-vm: [check-live] PASS after 15s [allowlisted=1]
charly check run check-charly-vm: [update] PASS after 2m27s [allowlisted=9]
charly check run check-charly-vm: [check-live-rebuild] PASS after 15s [allowlisted=1]
charly check run check-charly-vm: [cleanup] PASS after 8s [allowlisted=1]
charly check run check-charly-vm: [cleanup-members] PASS
charly check run check-charly-vm: PASS (steps=9)
```

summary.yml: `errors: 0, warnings: 0, allowlisted: 19` (the documented `dev-worktree-binary-notice` + `pacman-pacnew-config-notice` + `pacman-needed-package-already-current` allowances). The changed path (the ledger read via `ledgerDeployIDs`/`ledgerCandyNames`) ran live on the fresh rebuild — the bed's deploy-add/update steps write the install ledger to the VM's charly.yml `ledger:` section, which this collector reads.

## Rulebook compliance

| Rule | How |
|---|---|
| R1 — RCA every anomaly | The install ledger was per-deploy JSON files under `~/.config/opencharly/installed/` — the user directive to unify all local state into `~/.config/charly/charly.yml` with proper kinds (sdk#183). |
| R2 — no pre-existing split | N/A — single cutover, no split. |
| R3 — no duplication | N/A — the plugin uses the sdk's one ledger path. |
| R4 — no workarounds | The fix is the real relocation, not a shim. |
| R5 — hard cutover | The JSON-file ledger path is replaced in the same PR; the sdk dropped the legacy `LedgerPaths` fields (a stale consumer fails to compile loudly). |
| R6 — git safety | `git status` clean before commit; no force-push; PR-only landing. |
| R7 — prove the gate | `go build` + `go test` pass on the final tree; the disposable bed run is pasted above. |
| R8 — generation/label verification | N/A — no generated artifacts. |
| R9 — binary integrity | The charly binary was rebuilt from the committed tree with the plugin replace; the bed installed and ran it on a fresh Arch VM. |
| R10 — disposable fresh-rebuild proof | The `check-charly-vm` disposable bed ran the changed ledger-read path on a fresh rebuild (above) — ZERO errors, ZERO warnings (19 documented allowances). |
| RDD — prove high-risk assumptions | The ledger read is exercised by the updated tests + the live bed run. |
| ADE — spec-is-the-test | N/A — no plan: acceptance steps. |
| SDD — schema-driven design | The `ledger:` kind is CUE-sourced (spec#66). |

## Change classification

- **Change class:** code/config (ledger relocation)
- **Verification gate:** build + unit tests + the `check-charly-vm` disposable bed run (fresh rebuild, ZERO errors/warnings)
- **Attribution tier:** `fully tested and validated` — the changed ledger-read path ran live on a disposable fresh-rebuild bed with retained output (above), and the unit suite passes from the committed tree.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*