# 2026.266.1251 — feat(status): migrate the guest-probe cache to spec/cache.Store + read EngineName

## Summary

Consumer leg of the **engine provider class** cutover (spec#144/#145, sdk#272 — both merged) for `plugin-substrate`.

1. **Guest-probe cache → `spec/cache.Store`.** The merged spec carries the generalized `spec/cache.Store`; the old top-level `cache.Read`/`cache.Write` are gone. `probeCachePath()` → `probeCacheStore()` returning `cache.OpenNamed("probes")`, with `Store.ReadTTL` / `Store.WriteValue` (same key, same 30s TTL).
2. **Engine is now `#EngineName`.** `deployEngineForBox` converts explicitly (`string(entry.Engine)`).
- spec pin `v0.2026250.523` → `v0.2026266.545`; sdk `v0.2026250.527` → `v0.2026266.623`. The `go.sum` also completes the standalone (workspace-masked) transitive entries.
- New regression test `probe_cache_store_test.go`.

### Coordination (read in full; the thread's concerns are addressed below)
The cache-store session's note raises two points: (a) **spec#147** replaces `cache.Store` with an OCI-Image-Layout `ArtifactStore` — they state *no objection to this PR landing first* and will migrate the `probes` consumer in their own sweep (opencharly/charly#632); (b) the engine-cutover R10 bed they name is **`check-substrate`** — which this body now runs by name (below). Their own `deploy-add` RCA (a poisoned `charly@main` remote cache) is a separate mechanism from the `GateSchemaVersion` skew this wave fixes; both are logged on opencharly/charly#633.

## How tested

**Change-class gate — the named R10 bed `check-substrate`** (vm, `disposable: true`), branch-built engine-cutover charly `2026.266.0826`, wave legs composed via `CHARLY_REPO_OVERRIDE`:
```
$ charly check run check-substrate
[vm-build] PASS 20.1s   [vm-create] PASS 21.829s   [deploy-add] PASS 54.786s
[bring-up-members] PASS 35.85s   [check-live] PASS 35.479s
[rebuild-members-down] PASS 23.639s   [update] PASS 2m28.065s   # fresh rebuild
[re-bring-up-members] PASS 35.238s   [rebuild-ready] PASS 931ms
[check-live-rebuild] PASS 32.154s   [cleanup] PASS 26.494s   [cleanup-members] PASS 28.447s
PASS (steps=12)
```
`.check/check-substrate/2026.266.1115/summary.yml` → `ok: true`, 0 failed steps.

**Supporting beds** (also `disposable: true`, fresh rebuild):
```
$ charly check run check-commands-local   → PASS (steps=6)   # status-runs + reap-orphans-runs
$ charly check run check-boxload-pod      → PASS (steps=14)
```

**Disposable target:** all three beds carry `disposable: true`.

**Changed-path execution:** the guest-probe cache + status paths execute in the substrate/commands beds; the `deployEngineForBox` `string(entry.Engine)` line sits inside `if … entry.Engine != ""` and these beds author no engine, so that one branch did not fire (compile-enforced). Honest tier: `analysed on a live system`.

Local gates (standalone, `GOWORK=off`): `gofmt -l .` (empty) · `go build ./...` · `go vet ./...` · `go test ./...` (`ok … 1.359s`, incl. `TestProbeCacheStore*`).

**R5 sweep (B14(d) — executed, covering every identifier this PR removes/renames):**
```
$ grep -rn "cache\.Read(\|cache\.Write(\|probeCachePath" --include='*.go' candy/plugin-substrate
(no matches)
$ grep -n "OpenNamed\|ReadTTL\|WriteValue" candy/plugin-substrate/status_probes.go
349:  if store.ReadTTL(key, probeCacheTTL, &cached) {
358:  store.WriteValue(key, results)
371:  return cache.OpenNamed("probes")
$ grep -n "string(entry.Engine)" candy/plugin-substrate/status_android_collect.go
358:  return string(entry.Engine)
```

## Project-rulebook rule-compliance

- **R0** — check/plugin/git-workflow skills loaded first.
- **R1** — the bed failure was root-caused (GateSchemaVersion skew + the separate poisoned-cache mechanism), not called environmental.
- **R2** — the wave + the orthogonal spec#147 cutover are named (#633/#632).
- **R3** — one named-store idiom; no second cache path.
- **R4** — no shim.
- **R5** — `cache.Read`/`cache.Write`/`probeCachePath` are gone; the executed sweep above covers each.
- **R6/R7** — green build/vet/test + the real beds.
- **R8** — no emitted artifact changed.
- **R9** — the beds run the branch binary `2026.266.0826`.
- **R10** — `check-substrate` (the named bed) fresh-rebuild cycle above, zero failed steps.
- **RDD** — the store round-trip + TTL are unit-exercised; the command paths ran live.
- **ADE** — the candy's baked plan ran.
- **Disposable-only autonomy** — only the beds' own `disposable: true` teardown.

## Change classification

Plugin Go code on a runtime path (guest-probe cache) + `EngineName` read + pin bump + one new test. Verification gate: the named `check-substrate` disposable R10 bed (fresh rebuild) + `check-substrate`/`check-commands-local`/`check-boxload-pod` + standalone Go gates. Attribution tier: `analysed on a live system` (the command paths ran live; the one non-default `#EngineName` branch did not fire).

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (analysed on a live system)*