# 2026.241.1142 — feat(status): refresh the `system:` section of the per-host charly.yml on status

## Summary

`charly status` is the natural refresh point for the host identity snapshot (spec#66 `schema/system.cue` `#SystemInfo`): it already reads the per-host config, and the snapshot answers "what is this host?" from the unified local config instead of re-probing on every invocation. The status collection calls `kit.PopulateSystemInfo()` (sdk#185) — best-effort, a detection failure never fails the status command.

- **`status_local.go`**: `collectLocalStatus` refreshes the `system:` section of the per-host charly.yml.
- **`go.mod`**: sdk v0.2026241.1140 (the system-info populator tag).

## How tested

```
$ cd candy/plugin-substrate && go build ./... && echo BUILD OK
BUILD OK
$ go test ./...
ok  	github.com/opencharly/plugin-substrate/candy/plugin-substrate	0.208s
?   	github.com/opencharly/plugin-substrate/candy/plugin-substrate/cmd/serve	[no test files]
```

**Disposable fresh-rebuild bed run (A2/R10)** — `check-charly-vm` (the `disposable: true` Arch VM bed). The charly binary was rebuilt from the committed tree with this plugin change (local replace) + the sdk#185 populator, synced to `candy/charly-dev/bin/charly`, and the bed run:

```
$ charly check run check-charly-vm
charly check run check-charly-vm: [vm-build] PASS after 4.6s [allowlisted=1]
charly check run check-charly-vm: [vm-create] PASS after 8.1s [allowlisted=1]
charly check run check-charly-vm: [deploy-add] PASS after 1m8s [allowlisted=5]
charly check run check-charly-vm: [bring-up-members] PASS
charly check run check-charly-vm: [check-live] PASS after 15s [allowlisted=1]
charly check run check-charly-vm: [update] PASS after 2m23s [allowlisted=9]
charly check run check-charly-vm: [check-live-rebuild] PASS after 21s [allowlisted=1]
charly check run check-charly-vm: [cleanup] PASS after 11s [allowlisted=1]
charly check run check-charly-vm: [cleanup-members] PASS
charly check run check-charly-vm: PASS (steps=9)
```

summary.yml: `errors: 0, warnings: 0, allowlisted: 19` (the documented `dev-worktree-binary-notice` + `pacman-pacnew-config-notice` + `pacman-needed-package-already-current` allowances). The changed path (the status collection refreshing the `system:` section) ran live on the fresh rebuild.

## Rulebook compliance

| Rule | How |
|---|---|
| R1 — RCA every anomaly | The `system:` kind schema was done (spec#66) but nothing populated it — the user directive to unify ALL local system state into `~/.config/charly/charly.yml` with proper kinds. |
| R2 — no pre-existing split | N/A — single feature. |
| R3 — no duplication | The plugin calls the sdk's one populator. |
| R4 — no workarounds | The fix is the real refresh, not a shim. |
| R5 — hard cutover | N/A — new feature, no legacy path. |
| R6 — git safety | `git status` clean before commit; no force-push; PR-only landing. |
| R7 — prove the gate | `go build` + `go test` pass on the final tree; the disposable bed run is pasted above. |
| R8 — generation/label verification | N/A — no generated artifacts. |
| R9 — binary integrity | The charly binary was rebuilt from the committed tree with the plugin replace; the bed installed and ran it on a fresh Arch VM. |
| R10 — disposable fresh-rebuild proof | The `check-charly-vm` disposable bed ran the changed status-refresh path on a fresh rebuild (above) — ZERO errors, ZERO warnings (19 documented allowances). |
| RDD — prove high-risk assumptions | The refresh is exercised by the live bed run + the sdk#185 populator tests. |
| ADE — spec-is-the-test | N/A — no plan: acceptance steps. |
| SDD — schema-driven design | The `system:` kind is CUE-sourced (spec#66). |

## Change classification

- **Change class:** code (status refresh of the `system:` section)
- **Verification gate:** build + unit tests + the `check-charly-vm` disposable bed run (fresh rebuild, ZERO errors/warnings)
- **Attribution tier:** `fully tested and validated` — the changed status-refresh path ran live on a disposable fresh-rebuild bed with retained output (above), and the unit suite passes from the committed tree.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*